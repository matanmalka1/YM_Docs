# Useless wrappers and aliases

Six confirmed items add naming or barrel indirection without enforcing a domain contract, accessibility behavior, permissions, analytics, layout, or another invariant. `RowActionButton.danger` is tracked once as canonical dead-code finding `DC-18` and is intentionally not counted here.

| Severity | Wrapper | Wrapped abstraction | Added value | Consumers | Recommendation |
|---|---|---|---|---|---|
| P3 · Confirmed | `frontend/src/features/notifications/hooks/useNotificationBell.ts:3-9` — `useNotificationBell` | One `useState(false)` with `openDrawer()` / `closeDrawer()` aliases | None. It adds no notification rule, persistence, permission, analytics, or focus behavior. The feature re-exports it at `notifications/index.ts:6`, creating a public dependency for local state. No hook/component test exists. | Exactly one: `frontend/src/components/layout/NotificationBell.tsx:4,7-23`. | Inline the boolean state and two setters in `NotificationBell`; delete the hook and public export. Expected behavior change: none. Risk: negligible. Verify the bell opens/closes the existing drawer. |
| P2 · Confirmed | `frontend/src/hooks/useDetailQuery.ts:3-14` — `useDetailQuery<TData>` | TanStack `useQuery()` with `queryKey`, `queryFn`, and `enabled: !!id` | No stable key, stale-time policy, error contract, or domain behavior. Its generic API hides the caller's unsafe `reportId as number` and response cast at `annualReports/hooks/useReportDetail.ts:9-13`. No tests cover either hook. | Exactly one: `frontend/src/features/annualReports/hooks/useReportDetail.ts:3,9-14`. | Inline a typed `useQuery` in `useReportDetail`, guard the ID inside `queryFn`, and reconcile `AnnualReportDetail` with the API response instead of casting. Delete the top-level hook. Behavior change: none intended. Risk: low; the type correction may expose a real response mismatch. |
| P2 · Confirmed | `frontend/src/features/annualReports/components/shared/CreateReportModalParts.tsx:94-102` — `SelectOptions` | `<Select label={label} error={error} options={options} {...registerProps} />` | None beyond prop forwarding. `registerProps: object` erases React Hook Form types and helps conceal that `Select` drops the registration ref. There is no annual-report semantic or invariant and no modal-parts test. | Four call sites, all in one component: `frontend/src/features/annualReports/components/shared/CreateReportModal.tsx:83,90,97,103`. | Inline the four local fields or render a typed local field definition; use `UseFormRegisterReturn`/`Controller`, not `object`. Delete `SelectOptions`. Behavior change: none intended. Risk: low; test field registration and validation while migrating. |
| P3 · Confirmed | `frontend/src/features/signatureRequests/utils.ts:4` — `signatureRequestStatusVariants` | Re-export/rename of `SIGNATURE_REQUEST_STATUS_VARIANTS` from `signatureRequests/constants.ts:90-96` | None. The second name suggests a second owner and makes status-map searches ambiguous. `SignatureRequestAuditDrawer.test.tsx` renders one consumer but does not depend on the alias name. | Complete set: `components/list/SignatureRequestRow.tsx:9,73`; `components/shared/SignatureRequestsDashboardPanel.tsx:18,95,185`; `components/drawer/SignatureRequestAuditDrawer.tsx:18,88`. | Import `SIGNATURE_REQUEST_STATUS_VARIANTS` from the canonical constants module and remove the alias export. Behavior change: none. Risk: negligible; existing drawer test plus typecheck is sufficient. |
| P3 · Confirmed | `frontend/src/features/documents/constants.ts:79` — `SEARCH_PLACEHOLDER` | Rename of app constant `DOCUMENT_SEARCH_PLACEHOLDER` from `frontend/src/constants/searchPlaceholders.constants.ts:23` | None. The generic local name obscures that the value already has a canonical document-specific owner. No document-filter test exists. | Exactly one: `frontend/src/features/documents/components/list/DocumentsFilterPanel.tsx:4,12`. | Import `DOCUMENT_SEARCH_PLACEHOLDER` directly and delete `SEARCH_PLACEHOLDER`. Behavior change: none. Risk: negligible. |
| P2 · Confirmed | **UW-06** — `frontend/src/components/ui/index.ts:1-6`, root UI mega-barrel | Six area barrels: `primitives`, `inputs`, `layout`, `overlays`, `table`, and `feedback` | None beyond a second export-star layer. It exposes every UI symbol through one ambiguous root despite having only one consumer, and violates the codebase preference for intentional imports. No test relies on the barrel itself. | Sole consumer: `frontend/src/features/binders/components/drawer/BinderReceivePanel.tsx:3,4,7,9`, importing `Alert`, `Button`, `DatePicker`, and `Textarea`. All other frontend code already uses area barrels or direct files. | Replace the four root imports with `@/components/ui/overlays`, `@/components/ui/primitives`, and `@/components/ui/inputs` (or direct modules), then delete `components/ui/index.ts`. Behavior change: none. Risk: negligible; typecheck and binder drawer smoke coverage are sufficient. |
