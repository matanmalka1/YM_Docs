# Dead code

Audit date: 2026-07-22. Scope: `frontend/src/**`. This report distinguishes dead declarations and public surfaces from dead files: all 840 production TypeScript/TSX/CSS files are reachable from `src/main.tsx`, and Knip JSON contains zero unused files under the canonical `src/` tree. (A full current Knip run also sees an unrelated untracked `.claude/worktrees/table-refactor/` copy outside scope.) The sole whole-file deletion candidate is ignored Finder metadata. Evidence combines TypeScript-aware import/re-export/dynamic-import reachability, exhaustive direct/computed/destructured object-member searches, barrel and route inspection, test inspection, and git-visible replacement history where relevant. Validation passed with 28 Vitest files / 107 tests and `npm run arch:check:strict`; the latter's alias blind spot is documented as `SQ-01` rather than treated as proof of clean topology.

Confirmed findings: **20** — P0: 0, P1: 0, P2: 9, P3: 11. Confirmed dead production-code files: **0**. Whole-file deletion candidates: **1**. Estimated low-risk removable volume is approximately **180 source lines**, plus a 6,148-byte `.DS_Store`.

| Severity | File | Symbol | Evidence | Consumers | Recommendation | Risk |
| --- | --- | --- | --- | --- | --- | --- |
| P3 | `frontend/src/features/notifications/.DS_Store` (binary; 6,148 bytes) | **DC-01 — Finder metadata file** | **Responsibility:** macOS Finder directory metadata; no application responsibility.<br>**Confidence:** Confirmed.<br>It is the only file returned by `find src -type f` but omitted by `rg --files src`; Git reports it ignored, and TypeScript, Vite, Vitest, Knip, and the import graph exclude it.<br>**Tests:** not applicable. | None. It is not imported, bundled, tested, or tracked. | Delete the local artifact and retain the existing ignore rule. | None; non-code metadata only. |
| P2 | `frontend/src/features/annualReports/api/annualReports.api.ts:55-63`<br>`frontend/src/features/annualReports/api/annualReports.status.api.ts:24-30`<br>`frontend/src/features/annualReports/api/annualReports.tax.api.ts:17-20` | **DC-02 — `annualReportsApi.listReportCharges`; `annualReportStatusApi.updateDeadline`; `annualReportTaxApi.getAdvancesSummary`** | **Responsibility:** typed transports for report charges, deadline updates, and advances summaries.<br>**Confidence:** Confirmed.<br>Exhaustive member tracing found no call, alias, bracket access, destructuring, hook, route, component, or test consumer. Their only downstream declarations are cascade-only endpoint/key/response members. Live screens instead use `getReport`, `listClientReports`, `getReadiness`, tax-calculation methods, and `annualReportsQK.detail`.<br>**Tests:** no API test covers these methods; existing annual-report tests do not reference them. | No real consumers. Cascade-only references: `ANNUAL_REPORT_ENDPOINTS.charges` (`api/endpoints.ts:9`), `ANNUAL_REPORT_ENDPOINTS.deadline` (`api/endpoints.ts:7`), `ANNUAL_REPORT_ENDPOINTS.advancesSummary` (`api/endpoints.ts:17`), `annualReportsQK.advancesSummary` (`api/queryKeys.ts:19`), and `AnnualReportChargesResponse` (`api/contracts.ts:202`). `AdvancesSummary` remains live in `utils/panelHelpers.ts:1,17`. | Remove the three methods and only their cascade-only endpoint/key/type members. Preserve `AdvancesSummary` or narrow it because `panelHelpers` consumes it. | Low; no runtime call path. Backend endpoint removal is out of scope. |
| P2 | `frontend/src/features/annualReports/api/endpoints.ts:4`<br>`frontend/src/features/annualReports/api/queryKeys.ts:6-7,17` | **DC-03 — `ANNUAL_REPORT_ENDPOINTS.transition`; `annualReportsQK.reportCharges`; `annualReportsQK.reportDetails`** | **Responsibility:** an older transition URL and intent-named cache keys.<br>**Confidence:** Confirmed.<br>Each member occurs only at its declaration. Live status mutations call `ANNUAL_REPORT_ENDPOINTS.status`; live hooks use the base `annualReportsQK.detail(...)`. No computed, destructured, dynamic, or test access exists.<br>**Tests:** no direct coverage; no test update required. | None in production or tests. | Delete the three members. Treat `transition` as obsolete frontend compatibility, not as a decision about the backend route. | Low. |
| P3 | `frontend/src/features/annualReports/types.ts:7` | **DC-04 — `CURRENT_YEAR`** | **Responsibility:** computes the current year at module load.<br>**Confidence:** Confirmed.<br>Knip reports the export unused and repository-wide symbol search finds only this declaration. Other features use separate, live local values.<br>**Tests:** none. | None. | Delete this declaration; retain the rest of `types.ts`. | None to very low. |
| P2 | `frontend/src/features/binders/api/binders.api.ts:88-93,111-121`<br>`frontend/src/features/binders/api/endpoints.ts:14-15`<br>`frontend/src/features/binders/api/queryKeys.ts:6`<br>`frontend/src/features/binders/types.ts:69-78`<br>`frontend/src/features/binders/api/contracts.ts:58-64` | **DC-05 — `bindersApi.getOpenBinders`; `bindersApi.updateIntake` and cascade-only declarations** | **Responsibility:** specialized open-binder listing and intake editing.<br>**Confidence:** Confirmed.<br>No method consumer exists. Live open-binder lookups call `bindersApi.list(...)`; live intake UI reads via `getIntakes`, and no edit action is rendered.<br>**Tests:** no API/query-key test covers these symbols. | No real consumers. `BINDER_ENDPOINTS.bindersOpen`, `bindersQK.open`, and `ListOpenBindersParams` are consumed only by `getOpenBinders`; `BINDER_ENDPOINTS.binderIntakeById` and `BinderIntakeUpdatePayload` are consumed only by `updateIntake`. Replacement consumers are `vatReports/hooks/useActiveVatBinder.ts:9` and `binders/hooks/useReceiveBinderDrawer.ts:116`, both using `bindersApi.list`. | Remove both methods and their cascade-only endpoint/key/contracts. Preserve the generic list and intake-read paths. | Low statically; product confirmation is prudent only if intake editing is an intentionally dormant upcoming feature. |
| P3 | `frontend/src/features/binders/api/queryKeys.ts:7`<br>`frontend/src/features/binders/index.ts:13-18` | **DC-06 — `bindersQK.forClient`; unused binder root re-exports** | **Responsibility:** client key root and feature-boundary exposure of two guards, two option lists, and two status types.<br>**Confidence:** Confirmed.<br>`forClient` has no access; `forClientPage` is live. Knip reports all six root exports unused, while direct internal imports prove their source declarations remain live.<br>**Tests:** no direct barrel/key coverage. | No consumer of `forClient` or the root exports. Underlying guards/options are used by `hooks/useBindersFilters.ts:4,17-18` and `hooks/useBindersPage.ts:13,37-38`; underlying types remain live in binder contracts/types. | Delete `forClient` and narrow `index.ts`; do not delete the underlying guards, options, or types. | Very low. |
| P2 | `frontend/src/features/clients/api/clients.api.ts:156-159`<br>`frontend/src/features/businesses/api/endpoints.ts:4-5`<br>`frontend/src/features/clients/api/endpoints.ts:20,22-23,25-26,28,31` | **DC-07 — `clientsApi.restoreBusiness`; `BUSINESS_ENDPOINTS.businessRestore`; six `CLIENT_ROUTES` builders** | **Responsibility:** restore a deleted business and construct charge/VAT/tax-calendar/advance-payment/annual-report/communication client sub-routes.<br>**Confidence:** Confirmed.<br>No hook, component, page, route, test, indirect access, or computed access consumes them. Live client navigation uses `CLIENT_ROUTES.tab` or live tab-specific builders; charge detail uses `CHARGE_ROUTES.clientDetail`; the business UI exposes create/update/delete but no restore action.<br>**Tests:** no restoration test; client query-key tests do not cover these routes. | `restoreBusiness` has no consumer; `businessRestore` is referenced only by that dead method. `CLIENT_ROUTES.chargeDetail`, `vatDetail`, `taxCalendar`, `advancePaymentDetail`, `annualReportDetail`, and `communication` have no consumers. The matching `AppRoutes.tsx` route declarations are live and are not deletion candidates. | Remove the dead API/endpoint pair and six unused route-builder members; keep the actual route declarations. | Low. |
| P3 | `frontend/src/features/clients/api/queryKeys.ts:10,16`<br>`frontend/src/features/clients/index.ts:5,7,10-11,32` | **DC-08 — `clientsQK.taxProfile`; `clientsQK.firstBusiness`; unused clients root re-exports** | **Responsibility:** intent-named keys and public exposure of client options, parsers, and types.<br>**Confidence:** Confirmed.<br>The key members have no access. Knip identifies the six public exports as unused, while their source declarations remain live internally.<br>**Tests:** client query-key tests cover `creationImpact` and `businesses`, not these members. | No consumers of the two key members or root exports. Live replacements: `taxProfile/hooks/useTaxProfile.ts:10` uses `clientsQK.detail`; `hooks/useFirstBusinessId.ts:7` uses `clientsQK.businessesAll`. Underlying options/parsers/types are consumed inside clients hooks/forms/contracts. | Remove the two key members and narrow only the root barrel. | Very low. |
| P2 | `frontend/src/features/vatReports/api/vatReports.api.ts:29-34,60-63,117-122`<br>`frontend/src/features/vatReports/api/contracts.ts:103-122,162-164`<br>`frontend/src/features/vatReports/api/endpoints.ts:5` | **DC-09 — `vatReportsApi.list`; `vatReportsApi.updateWorkItem`; `vatReportsApi.listByClient` and cascade-only declarations** | **Responsibility:** flat work-item listing, generic PATCH, and client-scoped paginated listing.<br>**Confidence:** Confirmed.<br>Exhaustive tracing found no consumer. The active VAT page uses grouped/status-summary APIs; client VAT UI uses `getClientSummary`; mutations use explicit workflow and invoice endpoints.<br>**Tests:** no VAT API test covers these methods; VAT helper tests are unrelated. | No real consumers. `VatWorkItemsListParams`, `UpdateVatWorkItemPayload`, `VatClientWorkItemsParams`, and `VAT_ENDPOINTS.vatWorkItemsByClient` are referenced only by these dead methods. `VatWorkItemListResponse` remains live elsewhere. | Remove the three methods and cascade-only types/endpoint. Preserve `VatWorkItemListResponse`. | Low. |
| P3 | `frontend/src/features/vatReports/constants/vatConstants.ts:149-153` | **DC-10 — `CATEGORY_TABLE_LABELS`** | **Responsibility:** clone of `CATEGORY_LABELS` with a different travel label.<br>**Confidence:** Confirmed.<br>Knip reports it unused and exact symbol search finds only its declaration/comment; `CATEGORY_LABELS` is the live map.<br>**Tests:** none. | None. | Delete the comment and map. | None to very low. |
| P2 | `frontend/src/features/documents/api/documents.api.ts:51-54`<br>`frontend/src/features/documents/api/endpoints.ts:6`<br>`frontend/src/features/documents/api/queryKeys.ts:8` | **DC-11 — `documentsApi.listByAnnualReport`; `DOCUMENT_ENDPOINTS.documentsByAnnualReport`; `documentsQK.byAnnualReport`** | **Responsibility:** list documents directly by annual-report ID and key the response.<br>**Confidence:** Confirmed.<br>No production or test reference exists. Live annual-report interactions use document IDs/download URLs or client document lists.<br>**Tests:** none. | None. `DocumentVersionsResponse` is not part of the dead cascade; it remains consumed by `getVersions`. | Remove the method, endpoint member, and key member; retain shared live response types. | Low. |
| P2 | `frontend/src/features/authorityContacts/api/authorityContacts.api.ts:44-49`<br>`frontend/src/features/authorityContacts/api/queryKeys.ts:7`<br>`frontend/src/features/authorityContacts/api/queryKeys.test.ts:27-29` | **DC-12 — `authorityContactsApi.getAuthorityContact`; `authorityContactsQK.detail`** | **Responsibility:** fetch and cache one authority contact.<br>**Confidence:** Confirmed.<br>The production graph has zero references. The cache key's only non-declaration occurrence is a unit assertion; edit/delete flows operate from the already-loaded list row and use the by-ID endpoint directly for mutations.<br>**Tests:** one test-only key assertion must be removed or updated; the method itself has no test. | No runtime consumer. Sole key consumer: `authorityContacts/api/queryKeys.test.ts:27-29`. `AUTHORITY_CONTACT_ENDPOINTS.authorityContactById` remains live in update/delete calls. | Remove the method and key, and remove the now-meaningless test assertion. Keep the shared by-ID endpoint. | Low. |
| P3 | `frontend/src/features/users/api/users.api.ts:23-26` | **DC-13 — `usersApi.getById`** | **Responsibility:** fetch one user record.<br>**Confidence:** Confirmed.<br>The containing API object is live, but this member appears only at its declaration; exhaustive tracing found no indirect access. Users edit/reset/activation flows use list-row data and other API members.<br>**Tests:** none. | None. `USER_ENDPOINTS.userById` is still consumed by the live update method. | Delete only `getById`; keep the endpoint. | Very low. |
| P3 | `frontend/src/features/correspondence/api/queryKeys.ts:2`<br>`frontend/src/features/signatureRequests/api/queryKeys.ts:4`<br>`frontend/src/features/tasks/api/queryKeys.ts:19`<br>`frontend/src/features/taxDashboard/api/queryKeys.ts:3`<br>`frontend/src/features/timeline/api/queryKeys.ts:3` | **DC-14 — `correspondenceQK.forBusiness`; `signatureRequestsQK.businessNamesBatch`; `tasksQK.lists`; `taxDashboardQK.urgentDeadlines`; `timelineQK.clientRoot`** | **Responsibility:** cache-key roots for absent or superseded data flows.<br>**Confidence:** Confirmed.<br>No direct, computed, destructured, runtime, or test access exists. Live behavior uses different keys: correspondence `forClient`, task `all`/`list`, tax dashboard `submissions`, and timeline `clientEvents`; signature-request names come from loaded records.<br>**Tests:** none. | None. | Delete the five members independently. | Very low. |
| P3 | `frontend/src/api/core-endpoints.ts:2-3` | **DC-15 — `CORE_ENDPOINTS.health`; `CORE_ENDPOINTS.info`** | **Responsibility:** URL constants for health/info endpoints.<br>**Confidence:** Confirmed.<br>No direct, computed, destructured, or test reference exists. The file remains live for six auth URLs used by `frontend/src/api/auth.api.ts:41-76`.<br>**Tests:** none. | None for these two properties; six sibling auth members are live consumers of the containing object. | Remove only `health` and `info`; retain the file and auth members. | None to very low. |
| P3 | `frontend/src/features/charges/api/index.ts:3`<br>`frontend/src/features/charges/index.ts:13` | **DC-16 — re-exported `CHARGE_ROUTES`; re-exported `ChargeListItem`** | **Responsibility:** API-subbarrel/root-feature public exposure.<br>**Confidence:** Confirmed.<br>Knip reports both exported surfaces unused. Live charges modules import the source route object/type directly.<br>**Tests:** no barrel-surface tests. | No API-barrel consumer of `CHARGE_ROUTES`; live pages/hooks import `../api/endpoints`. No cross-feature consumer imports `ChargeListItem` from the root; the underlying type remains heavily used within charges. | Narrow the two barrels only; retain the route object and type at their source modules. | Very low. |
| P3 | `frontend/src/hooks/useSearchParamFilters.ts:24-40`<br>`frontend/src/features/search/api/contracts.ts:46-49,63-68` | **DC-17 — exported `nextFilterParams`; `resetFilterParams`; `SearchMatchGroup`; `PaginatedClientMatches`** | **Responsibility:** live local URL helpers and response-composition types.<br>**Confidence:** Confirmed.<br>Knip reports only the exported surfaces unused; local references prove the declarations themselves are live. No external import or test consumes them.<br>**Tests:** no direct URL-helper test; search API tests exercise response behavior but do not import these types. | URL helpers are used only in the same file at lines 40, 56, 63, 70, and 77. Search types are used only in the same contracts file at lines 61 and 71. | Remove only the `export` modifiers. | None to very low. |
| P3 | `frontend/src/components/ui/table/toolbar/RowActions.tsx:355-379` | **DC-18 — `RowActionButtonProps.danger` and compatibility fallback** | **Responsibility:** deprecated boolean compatibility input that falls back to a danger tone.<br>**Confidence:** Confirmed.<br>All seven JSX calls were inspected: six pass `tone`, one uses the neutral default, and none passes/spreads `danger`. Git history shows `tone` replaced the button boolean on 2026-06-23, leaving this branch behind.<br>**Tests:** no `RowActionButton`/`RowActions` test exists. | Complete `RowActionButton` consumer set: `annualReports/components/annex/AnnexDataTable.tsx:61,68,96,104,110` and `annualReports/components/financials/FinancialLineRow.tsx:73,81`.<br>Important: the distinct `RowActionItem.danger` API at `RowActions.tsx:209-253,307` has 11 live callers across binders, charges, clients, documents, signature requests, tasks, users, VAT, and work queue; it is not dead. | Remove only `RowActionButtonProps.danger`, its default binding/comment, and fallback; resolve as `tone ?? 'neutral'`. Preserve `RowActionItem.danger`. | Very low; current rendered behavior is unchanged. |
| P2 | `frontend/src/features/reports/pages/AgingReportView.tsx:13-17,46-52,62` | **DC-19 — `AgingReportView.embedded` and embedded-only branches** | **Responsibility:** optional embedded presentation mode that suppresses the header and moves actions.<br>**Confidence:** Confirmed.<br>The sole runtime render omits the prop, so it is always `false`; no test, dynamic import, alias, or spread-prop call exists. Git history proves its only `<AgingReportView embedded />` consumer was added for a ChargesPage mode and removed 57 minutes later, while the stale branches survived.<br>**Tests:** the reports feature has no tests. | Public re-export: `frontend/src/features/reports/index.ts:4`. Sole runtime render: `frontend/src/router/AppRoutes.tsx:185` as `<AgingReportView />`. No other consumers. | Make the page prop-less, always render `PageHeader`, and remove the embedded-only action rendering. | Low; retains the only reachable `embedded=false` output. |
| P2 | `frontend/src/features/reports/pages/AnnualReportStatusView.tsx:7-13`<br>`frontend/src/features/reports/hooks/useAnnualReportStatusReport.ts:7-9,18` | **DC-20 — `taxYear`/`controlledYear`; inert `setTaxYear` state API** | **Responsibility:** controlled/uncontrolled year selection for the annual-status report.<br>**Confidence:** Confirmed.<br>The sole render passes no prop, so `controlledYear` is always undefined. `internalYear` initializes to the current year and cannot change because the returned setter is not consumed. Git history shows the visible selector was removed before this controlled prop was ever wired.<br>**Tests:** the reports feature has no tests. | Public re-export: `frontend/src/features/reports/index.ts:5`. Sole render: `frontend/src/router/AppRoutes.tsx:186` as `<AnnualReportStatusView />`. Sole hook call: `AnnualReportStatusView.tsx:13`; it does not destructure `setTaxYear`. Same-named setters in `ClientDocumentsTab.tsx` are unrelated local state. | Remove the view prop, hook parameter, `useState`, and returned setter; use one current-year value directly. If historical filtering is still required, restore it as a separately specified URL-backed behavior. | Low for deletion; runtime stays current-year-only. Product intent for historical-year selection needs confirmation before treating that limitation as desirable. |

## Verification boundaries

- No route was classified unreachable. Routes without navigation links remain valid direct-URL entry points.
- No stale feature flag was found.
- There are no confirmed abandoned production files; every production source/style file is reachable. The findings above are member-, prop-, branch-, or export-level except DC-01.
- DC-05's dormant intake-edit capability and DC-20's removed historical-year selection are statically dead, but product intent should be checked before deleting them in a future change.
